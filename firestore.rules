rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only server-side (Cloud Functions / admin SDK) access allowed
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
